## Network

**Definition:**
A network in computer terms is a group of devices connected together so they can communicate with each other. This connection is established using various technologies like cables or wireless signals.

**Example:**
Imagine you have a home network where your computer, printer, and smartphone are all connected. This forms a small network.

#### Netmask (Subnet Mask)

**Definition:**
A netmask, also known as a subnet mask, is used to divide an IP address into network and host portions. It helps in determining which part of an IP address represents the network and which part represents individual devices (hosts) on that network.

**Example:**

* **IP Address:** 192.168.1.10
* **Subnet Mask:** 255.255.255.0

In this example, the subnet mask `255.255.255.0` means the first 24 bits of the IP address are the network part, and the remaining 8 bits are for hosts.

#### Subnetting

**Definition:**
Subnetting is the process of dividing a larger network into smaller, more manageable sub-networks (subnets). It helps in organizing and optimizing the use of IP addresses.

**How It Works:**

* **Network Address:** Identifies the overall network.
* **Subnet Mask:** Defines the boundary between the network part and the host part of an IP address.
* **Subnet Address:** Identifies individual sub-networks within the larger network.

**Example:**
Suppose you have a network with the IP address `192.168.1.0` and a subnet mask of `255.255.255.0`. This gives you one subnet with 254 usable IP addresses.

If you subnet this into smaller networks, you could have:

* **Subnet 1:** `192.168.1.0/25` with IP range `192.168.1.1` to `192.168.1.126`
* **Subnet 2:** `192.168.1.128/25` with IP range `192.168.1.129` to `192.168.1.254`

---

#### Network

**Definition:**
A network in computer terms refers to a group of devices connected together so they can communicate with each other. This connection is established through various technologies such as cables or wireless signals.

**Example:**
Imagine that you have a home network with your computer, printer, and smartphone all connected. This forms a small network.

#### Netmask (Subnet Mask)

**Definition:**
A netmask, also known as a subnet mask, is used to divide an IP address into network and host portions. It helps in identifying which part of the IP address represents the network and which part represents individual devices (hosts) on that network.

**Example:**

* **IP Address:** 192.168.1.10
* **Subnet Mask:** 255.255.255.0

In this case, the subnet mask `255.255.255.0` means the first 24 bits of the IP address represent the network part, while the remaining 8 bits are for hosts.

#### Subnetting

**Definition:**
Subnetting is the process of dividing a larger network into smaller, more manageable sub-networks (subnets). It helps in organizing and optimizing the use of IP addresses.

**How It Works:**

* **Network Address:** Identifies the entire network.
* **Subnet Mask:** Defines the boundary between the network part and the host part of an IP address.
* **Subnet Address:** Identifies the individual sub-networks within the larger network.

**Example:**
Suppose you have a network with the IP address `192.168.1.0` and a subnet mask of `255.255.255.0`. This configuration creates one subnet with 254 usable IP addresses.

If you subnet this network into smaller networks, you would get:

* **Subnet 1:** `192.168.1.0/25` with IP range `192.168.1.1` to `192.168.1.126`
* **Subnet 2:** `192.168.1.128/25` with IP range `192.168.1.129` to `192.168.1.254`

---

**Reserved IP Addresses**

A reserved IP address is an IP address that is assigned to your account and remains available for you unless you release it. You can use reserved IP addresses for various purposes.

**Reserved IP Address**
A reserved IP address is an IP address that has been assigned to your account and is available for your use until you release it. You can use reserved IP addresses for different purposes.
